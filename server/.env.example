# Copy this file to ".env" and fill in your own Travelpayouts API token.
# Get it from your Travelpayouts dashboard -> Tools -> API (free, no payment required).
# Never commit the real .env file to git.

TRAVELPAYOUTS_API_TOKEN=your_token_here

PORT=8787
